# 📊 GRID BOT — Полный Анализ

> **Дата:** 2026-01-28  
> **Версия:** 1.0  
> **Статус:** ✅ Реализован и интегрирован

---

## 📋 СОДЕРЖАНИЕ

1. [Обзор](#обзор)
2. [Архитектура](#архитектура)
3. [Структура кода](#структура-кода)
4. [Конфигурации](#конфигурации)
5. [Интеграция](#интеграция)
6. [Telegram команды](#telegram-команды)
7. [Режимы работы](#режимы-работы)
8. [Статистика](#статистика)
9. [Примеры работы](#примеры-работы)

---

## 🎯 ОБЗОР

### Что такое Grid Bot?

Grid Bot — это торговый модуль, который размещает **сетку ордеров** выше и ниже текущей цены, зарабатывая на каждом движении рынка в обе стороны.

### Принцип работы

```
Текущая цена: $100,000

SELL ордера (выше):
├── $105,000 (SELL) ← продать дороже
├── $104,000 (SELL)
├── $103,000 (SELL)
├── $102,000 (SELL)
├── $101,000 (SELL)

─────────── $100,000 ───────────  ← текущая цена

BUY ордера (ниже):
├── $99,000 (BUY) ← купить дешевле
├── $98,000 (BUY)
├── $97,000 (BUY)
├── $96,000 (BUY)
└── $95,000 (BUY)
```

### Цикл сделки

```
1. Цена падает → BUY @ $99,000 срабатывает
2. Сразу ставим SELL @ $99,200 (+0.2%)
3. Цена растёт → SELL @ $99,200 срабатывает
4. = Профит +$200!
5. Ставим новый BUY @ $99,000
```

### Когда Grid Bot работает лучше всего?

✅ **ИДЕАЛЬНО:**
- Боковой рынок (sideways) — много движений туда-сюда
- Волатильность 2-5% в день
- Чёткие уровни поддержки/сопротивления

✅ **ХОРОШО:**
- Медленный тренд вверх/вниз
- Стабильные монеты (BTC, ETH)

⚠️ **ОСТОРОЖНО:**
- Сильный тренд вверх → много BUY, мало SELL
- Сильный тренд вниз → много SELL, мало BUY
- Низкая волатильность → мало сделок

---

## 🏗️ АРХИТЕКТУРА

### Структура файлов

```
app/
├── modules/
│   ├── base_module.py          # BaseModule, ModuleSignal
│   ├── grid_bot.py            # ⭐ Grid Bot основной модуль
│   └── __init__.py             # Экспорт grid_bot
│
├── core/
│   ├── monitor.py              # Интеграция Grid в главный цикл
│   ├── smart_notifications.py  # Уведомления Grid
│   └── live_updates.py         # Live updates Grid статуса
│
└── notifications/
    └── telegram_bot.py         # /grid команда
```

### Диаграмма потока данных

```
┌──────────────────────────────────────┐
│     MarketMonitor (главный цикл)     │
│          каждые 60 секунд            │
└──────────────┬───────────────────────┘
               │
               ↓
    📊 ШАГ 3: Grid Bot проверка
               │
               ↓
┌──────────────────────────────────────┐
│         grid_bot.get_signals()       │
│                                      │
│  Для каждой монеты (BTC, ETH...):   │
│  1. Если сетка не настроена:        │
│     └─ setup_grid(symbol, price)    │
│  2. Проверить исполнение:           │
│     └─ check_orders(symbol, price)  │
└──────────────┬───────────────────────┘
               │
               ↓ (если ордер исполнен)
┌──────────────────────────────────────┐
│        Создать ModuleSignal          │
│  direction: "BUY" или "SELL"         │
│  entry_price: цена исполнения        │
│  reason: "Grid BUY @ $99,000"        │
└──────────────┬───────────────────────┘
               │
               ↓
┌──────────────────────────────────────┐
│     Режим: signal или auto?          │
│                                      │
│  📢 SIGNAL:                          │
│  └─ smart_notifications.queue_signal│
│     └─ Telegram уведомление          │
│                                      │
│  🤖 AUTO:                            │
│  └─ _execute_grid_trade(signal)     │
│     ├─ Bybit API (real trading)     │
│     └─ Telegram уведомление          │
└──────────────────────────────────────┘
```

---

## 📦 СТРУКТУРА КОДА

### Основные классы

#### 1. **GridOrderStatus** (Enum)
```python
PENDING = "pending"      # Ждёт исполнения
FILLED = "filled"        # Исполнен
CANCELLED = "cancelled"  # Отменён
```

#### 2. **GridOrderSide** (Enum)
```python
BUY = "buy"
SELL = "sell"
```

#### 3. **GridOrder** (Dataclass)
```python
@dataclass
class GridOrder:
    id: str                          # Уникальный ID
    symbol: str                      # BTC, ETH...
    side: GridOrderSide              # BUY или SELL
    price: float                     # Цена ордера
    quantity: float                  # Количество монет
    status: GridOrderStatus          # Статус
    created_at: datetime
    filled_at: Optional[datetime]
    linked_order_id: Optional[str]  # Связанный ордер (BUY → SELL)
```

#### 4. **GridConfig** (Dataclass)
```python
@dataclass
class GridConfig:
    symbol: str
    enabled: bool = True
    
    # Диапазон сетки
    upper_price: float = 0.0
    lower_price: float = 0.0
    
    # Параметры сетки
    grid_count: int = 10              # Количество уровней
    grid_step_percent: float = 0.5    # Шаг сетки в %
    
    # Размер позиции
    order_size_usdt: float = 50.0     # Размер каждого ордера
    
    # Профит
    profit_per_grid: float = 0.3      # Профит с каждой сетки %
    
    # Лимиты
    max_open_orders: int = 20
    min_profit_usdt: float = 0.1
```

#### 5. **GridTrade** (Dataclass)
```python
@dataclass
class GridTrade:
    id: str
    symbol: str
    buy_price: float
    sell_price: float
    quantity: float
    profit_usdt: float
    profit_percent: float
    opened_at: datetime
    closed_at: datetime
```

#### 6. **GridBot** (Main Class)

**Атрибуты:**
```python
self.enabled = True
self.configs: Dict[str, GridConfig] = {}
self.orders: Dict[str, GridOrder] = {}
self.trades: List[GridTrade] = []
self.stats = {
    "total_trades": 0,
    "total_profit_usdt": 0.0,
    "today_trades": 0,
    "today_profit_usdt": 0.0,
    "last_trade_time": None,
}
self.paper_trading = True
```

**Основные методы:**

| Метод | Описание |
|-------|----------|
| `setup_grid(symbol, price)` | Настроить сетку вокруг текущей цены |
| `check_orders(symbol, price)` | Проверить исполнение ордеров |
| `get_signals(market_data)` | Получить сигналы (для интеграции) |
| `get_status()` | Статус модуля (dict) |
| `get_status_text()` | Текст для Telegram |
| `_create_initial_orders()` | Создать начальную сетку |
| `_create_counter_order()` | Создать обратный ордер |
| `_record_trade()` | Записать закрытую сделку |
| `reset_today_stats()` | Сброс дневной статистики |

---

## ⚙️ КОНФИГУРАЦИИ

### Дефолтные настройки для монет

#### BTC (большие ордера, малый шаг)
```python
GridConfig(
    symbol="BTC",
    grid_count=10,
    grid_step_percent=0.3,      # 0.3% = $300 при $100k
    order_size_usdt=100.0,      # $100 на ордер
    profit_per_grid=0.2,        # 0.2% профит
)
```

**Пример сетки:**
```
При BTC = $100,000:
Шаг = 0.3% = $300

SELL: $105,000, $104,700, $104,400, $104,100, $103,800...
BUY:  $99,700, $99,400, $99,100, $98,800, $98,500...

Профит на цикл: ~0.2% = $200
Ожидаемых сделок/день: 5-10 (в боковике)
```

#### ETH (средние параметры)
```python
GridConfig(
    symbol="ETH",
    grid_count=10,
    grid_step_percent=0.4,      # 0.4% = $14 при $3500
    order_size_usdt=75.0,       # $75 на ордер
    profit_per_grid=0.25,       # 0.25% профит
)
```

#### Альты (больший шаг, волатильнее)
```python
for symbol in ["SOL", "BNB", "XRP", "ADA", "DOGE", "LINK", "AVAX"]:
    GridConfig(
        symbol=symbol,
        grid_count=8,
        grid_step_percent=0.5,  # 0.5%
        order_size_usdt=50.0,   # $50 на ордер
        profit_per_grid=0.3,    # 0.3% профит
    )
```

### Сравнительная таблица

| Монета | Уровней | Шаг % | Размер $ | Профит % | Ожидаемо сделок/день |
|--------|---------|-------|----------|----------|----------------------|
| BTC    | 10      | 0.3%  | $100     | 0.2%     | 5-15                 |
| ETH    | 10      | 0.4%  | $75      | 0.25%    | 8-20                 |
| SOL    | 8       | 0.5%  | $50      | 0.3%     | 10-25                |
| BNB    | 8       | 0.5%  | $50      | 0.3%     | 8-20                 |
| XRP    | 8       | 0.5%  | $50      | 0.3%     | 10-30                |
| ADA    | 8       | 0.5%  | $50      | 0.3%     | 10-30                |
| DOGE   | 8       | 0.5%  | $50      | 0.3%     | 15-40                |
| LINK   | 8       | 0.5%  | $50      | 0.3%     | 8-20                 |
| AVAX   | 8       | 0.5%  | $50      | 0.3%     | 8-20                 |

---

## 🔗 ИНТЕГРАЦИЯ

### 1. Monitor Integration (`app/core/monitor.py`)

**Строка 30:** Импорт
```python
from app.modules.grid_bot import grid_bot
```

**Строка 80:** Module settings
```python
self.module_settings = {
    'grid': {'enabled': True, 'mode': 'signal'},
    ...
}
```

**Строки 506-521:** Основной цикл
```python
# ========================================
# 📊 ШАГ 3: Grid Bot
# ========================================
if self.is_module_enabled('grid'):
    try:
        grid_signals = await grid_bot.get_signals({"prices": prices})
        
        for signal in grid_signals:
            if self.can_auto_trade('grid'):
                # AUTO режим — исполняем сделку
                logger.info(f"📊 Grid AUTO: {signal.direction} {signal.symbol}")
                await self._execute_grid_trade(signal)
                await self._notify_grid_executed(signal)
            else:
                # SIGNAL режим — только уведомление
                logger.info(f"📊 Grid SIGNAL: {signal.direction} {signal.symbol}")
                await self._notify_grid_signal(signal)
                
    except Exception as e:
        logger.error(f"Grid Bot error: {e}")
```

**Строка 1305:** Execute trade
```python
async def _execute_grid_trade(self, signal):
    """Исполнить Grid сделку"""
    # TODO: Интеграция с Bybit API
    logger.info(f"📊 Grid trade executed: {signal.symbol} {signal.direction}")
```

### 2. Smart Notifications Integration

**`app/core/smart_notifications.py`**

**Строка 29:** Module type
```python
class ModuleType(Enum):
    GRID = "grid"
    ...
```

**Строка 459:** Queue method
```python
async def queue_grid_status(
    self,
    symbol: str,
    price: float,
    support: float,
    resistance: float
):
    """Добавить статус Grid Bot"""
    
    if not self._can_module_report(ModuleType.GRID):
        return
    
    text = f"""
📊 *GRID BOT*

{symbol} ${price:,.0f}

Уровни:
💚 ${support:,.0f} (покупка)
❤️ ${resistance:,.0f} (продажа)

Жду касания уровней...
"""
    
    msg = QueuedMessage(
        module=ModuleType.GRID,
        priority=MessagePriority.LOW,
        text=text.strip(),
        needs_ai=False
    )
    
    self._add_to_queue(msg)
```

### 3. Telegram Bot Integration

**`app/notifications/telegram_bot.py`**

**Строка 94:** Command registration
```python
BotCommand(command="grid", description="📊 Grid Bot статус")
```

**Handler (где-то в коде):**
```python
@self.dp.message(Command("grid"))
async def cmd_grid(message):
    """Показать статус Grid Bot"""
    text = grid_bot.get_status_text()
    await message.answer(text, parse_mode=ParseMode.MARKDOWN)
```

### 4. Module Export

**`app/modules/__init__.py`**
```python
from app.modules.grid_bot import grid_bot, GridBot

__all__ = [
    "grid_bot",
    "GridBot",
    ...
]
```

---

## 📱 TELEGRAM КОМАНДЫ

### `/grid` — Статус Grid Bot

**Вывод:**
```
📊 GRID BOT STATUS

🟢 Активен

📈 Сегодня:
├── Сделок: 12
└── Профит: $45.60

📊 Всего:
├── Сделок: 156
└── Профит: $624.80

🔄 Ордера:
├── BUY: 38
└── SELL: 42

🎯 Монеты: BTC, ETH, SOL, BNB, XRP, ADA, DOGE
```

---

## 🎛️ РЕЖИМЫ РАБОТЫ

### 1. **SIGNAL Mode** (по умолчанию)

Бот **НЕ торгует**, только уведомляет:

```
📊 GRID BOT — СИГНАЛ

🟢 Рекомендация: КУПИТЬ BTC

━━━━━━━━━━━━━━━━━━━━
💰 Цена: $99,000
🎯 Цель: $99,300 (+0.3%)
━━━━━━━━━━━━━━━━━━━━

💡 Сетка показывает хорошую точку.
Рекомендуем купить вручную.

⏰ 14:32:15
```

### 2. **AUTO Mode** (с API ключами)

Бот **торгует автоматически**:

```
📊 GRID BOT — СДЕЛКА

🟢 КУПИЛ BTC

━━━━━━━━━━━━━━━━━━━━
💰 Цена: $99,000
💵 Размер: $100
━━━━━━━━━━━━━━━━━━━━

📈 Сегодня: 12 сделок
💰 Профит: $45.60

⏰ 14:32:15
```

### Переключение режима

Через **WebApp** (`🎛 Панель управления`):
```
📊 Grid Bot:
  [📢 Signal] [🤖 Auto] ← выбрать режим
```

---

## 📊 СТАТИСТИКА

### Структура stats

```python
self.stats = {
    "total_trades": 156,          # Всего сделок
    "total_profit_usdt": 624.80,  # Всего профита
    "today_trades": 12,           # Сделок сегодня
    "today_profit_usdt": 45.60,   # Профит сегодня
    "last_trade_time": datetime   # Последняя сделка
}
```

### Расчёт профита

```python
def _record_trade(self, sell_order: GridOrder):
    buy_order = self.orders[sell_order.linked_order_id]
    
    profit_percent = (sell_order.price - buy_order.price) / buy_order.price * 100
    profit_usdt = sell_order.quantity * (sell_order.price - buy_order.price)
    
    # Сохраняем сделку
    trade = GridTrade(...)
    self.trades.append(trade)
    
    # Обновляем статистику
    self.stats["total_trades"] += 1
    self.stats["total_profit_usdt"] += profit_usdt
    self.stats["today_trades"] += 1
    self.stats["today_profit_usdt"] += profit_usdt
```

### Сброс дневной статистики

Вызывается в полночь (UTC):
```python
def reset_today_stats(self):
    self.stats["today_trades"] = 0
    self.stats["today_profit_usdt"] = 0.0
```

---

## 💡 ПРИМЕРЫ РАБОТЫ

### Пример 1: Боковой рынок (идеально)

```
День:  BTC колеблется $99,000 - $101,000
Сетка: BUY @ $99,000, SELL @ $99,300

09:00 → Цена $100,000 → сетка настроена
10:30 → Цена $99,000 → BUY сработал
10:31 → Создан SELL @ $99,300
11:15 → Цена $99,300 → SELL сработал → +$30
11:16 → Создан BUY @ $99,000
12:00 → Цена $99,000 → BUY сработал
12:01 → Создан SELL @ $99,300
...

Итого за день: 8 циклов × $30 = $240 профита
```

### Пример 2: Тренд вверх

```
День:  BTC растёт $99,000 → $105,000
Сетка: BUY @ $99,000-$100,500, SELL @ $100,500-$102,000

09:00 → Цена $100,000 → сетка настроена
10:00 → Цена $100,500 → SELL @ $100,500 сработал
11:00 → Цена $101,000 → SELL @ $101,000 сработал
12:00 → Цена $101,500 → SELL @ $101,500 сработал
...

Проблема: Много SELL, но нет BUY (цена выше сетки)
Решение: Сетка автоматически двигается вверх

Итого: Меньше циклов, но всё равно профит
```

### Пример 3: Низкая волатильность

```
День:  BTC застрял $100,000 - $100,200
Сетка: Шаг 0.3% = $300

09:00 → Цена $100,000 → сетка настроена
15:00 → Цена $100,100 (ещё не $100,300)
17:00 → Цена $100,050
20:00 → Цена $100,150

Итого за день: 0 сделок (движение < шага сетки)
```

---

## 🎯 ОЖИДАЕМЫЕ РЕЗУЛЬТАТЫ

### Оптимистичный сценарий (боковик)

| Параметр | Значение |
|----------|----------|
| Монет в сетке | 9 (BTC, ETH, SOL, BNB, XRP, ADA, DOGE, LINK, AVAX) |
| Сделок/день на монету | 10 |
| Всего сделок/день | 90 |
| Средний профит/сделка | $0.50 |
| Профит/день | **$45** |
| Профит/месяц | **$1,350** |

### Реалистичный сценарий (смешанный рынок)

| Параметр | Значение |
|----------|----------|
| Сделок/день | 30-50 |
| Профит/день | **$15-25** |
| Профит/месяц | **$450-750** |

### Пессимистичный сценарий (сильный тренд)

| Параметр | Значение |
|----------|----------|
| Сделок/день | 5-10 |
| Профит/день | **$3-8** |
| Профит/месяц | **$90-240** |

---

## ⚠️ ВАЖНЫЕ ЗАМЕЧАНИЯ

### 1. Paper Trading по умолчанию

```python
self.paper_trading = True
```

**Grid Bot НЕ торгует реально** без API ключей Bybit!

### 2. Комиссии не учитываются

Текущая реализация — **виртуальная**.  
В реальности нужно вычитать:
- Bybit Spot Fee: 0.1% за сделку
- Slippage: ~0.05%
- **Итого:** ~0.15% на цикл

**Реальный профит:**
```
Заявленный: 0.3%
Комиссии:  -0.15%
Реальный:   0.15%
```

### 3. Интеграция с Bybit API

Метод `_execute_grid_trade` — **TODO**:
```python
async def _execute_grid_trade(self, signal):
    """Исполнить Grid сделку"""
    # TODO: Интеграция с Bybit API
    # - Размещение лимитных ордеров
    - Отслеживание исполнения
    - Управление балансом
```

### 4. Риск застревания капитала

В сильном тренде:
- Все BUY ордера могут исполниться
- Деньги «застрянут» в позициях
- Придётся ждать отката

**Рекомендация:**
- Ограничить капитал на Grid: 20-30% от баланса
- Использовать стоп-лоссы для защиты

---

## 📝 ВЫВОДЫ

### ✅ Что реализовано:

1. ✅ Полная логика Grid Bot
2. ✅ Конфигурации для 9 монет
3. ✅ Отслеживание ордеров
4. ✅ Статистика и история
5. ✅ Интеграция в Monitor
6. ✅ Smart Notifications
7. ✅ Telegram команды
8. ✅ Режимы Signal/Auto

### 🔧 Что нужно доделать:

1. ⏳ Реальная интеграция с Bybit API
2. ⏳ Учёт комиссий
3. ⏳ Управление балансом
4. ⏳ Динамическая настройка сетки
5. ⏳ Стоп-лоссы для защиты
6. ⏳ Бэктестинг Grid стратегии

### 🎯 Рекомендации:

1. **Начать с Paper Trading** для тестирования
2. **Подключить Bybit API** для реальной торговли
3. **Ограничить капитал** на Grid (20-30%)
4. **Мониторить тренды** — в сильных трендах отключать
5. **Оптимизировать параметры** под каждую монету

---

**Создано:** 2026-01-28  
**Автор:** AI Assistant  
**Статус:** ✅ Полный анализ завершён
